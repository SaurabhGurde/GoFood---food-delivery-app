{
  "name": "gofood",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install && cd backend && npm install && cd ../gofood && npm install && npm run build",
    "start": "concurrently \"cd backend && nodemon index.js\" \"cd ./gofood && npm start\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
